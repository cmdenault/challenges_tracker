// the data ChallengeList component expects

// import the interface of Challenge obj
import { Challenge } from "../model/Challenge";


export interface ChallengeList {
    title: string
    items: Challenge[]
}